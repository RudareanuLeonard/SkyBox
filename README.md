# SkyBox
SkyBox is a Dropbox-inspired file sync system in C++. It supports client–server recursive file transfer over a TCP connection

You need at least C++20 to compile it. Otherwise you will get some errors.
